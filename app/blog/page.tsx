import Link from 'next/link'
import { ArrowLeft, ArrowUpRight } from 'lucide-react'
import { posts, formatDate } from '@/lib/posts'

export default function BlogPage() { const [featured, ...rest] = posts; return <main className="min-h-screen bg-background"><header className="border-b border-border"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5"><Link href="/" className="font-mono text-sm font-bold"><span className="text-primary">m</span>ukund<span className="text-muted-foreground">.mm</span></Link><Link href="/" className="flex items-center gap-2 text-xs text-muted-foreground hover:text-foreground"><ArrowLeft size={14} /> Back to portfolio</Link></div></header><div className="mx-auto max-w-6xl px-5 py-16 md:py-24"><div className="mb-12 flex items-end justify-between"><div><p className="mb-3 font-mono text-xs uppercase tracking-[0.18em] text-primary">Writing / Field notes</p><h1 className="text-5xl font-bold tracking-tight md:text-7xl">The notebook</h1><p className="mt-5 max-w-xl text-muted-foreground">Thinking out loud about AI, cloud infrastructure, product craft, and the systems in between.</p></div></div><nav className="mb-8 flex gap-2 overflow-x-auto pb-2 text-xs"><span className="rounded-full bg-primary px-4 py-2 font-semibold text-primary-foreground">All posts</span>{['AI', 'Cloud', 'Product', 'DevOps', 'Career'].map(tag => <span key={tag} className="rounded-full border border-border px-4 py-2 text-muted-foreground">{tag}</span>)}</nav><Link href={`/blog/${featured.slug}`} className="group relative mb-5 block min-h-[380px] overflow-hidden rounded-3xl border border-border bg-muted p-8 md:p-14"><div className="absolute inset-0 bg-gradient-to-br from-primary/15 via-transparent to-transparent" /><div className="relative flex h-full max-w-2xl flex-col justify-end"><p className="font-mono text-xs text-primary">Featured · {formatDate(featured.date)}</p><h2 className="mt-4 text-3xl font-bold tracking-tight md:text-5xl">{featured.title}</h2><p className="mt-4 max-w-lg text-sm leading-6 text-muted-foreground">{featured.excerpt}</p><p className="mt-7 flex items-center gap-2 text-xs font-semibold">Read article <ArrowUpRight size={14} /></p></div></Link><div className="grid gap-4 md:grid-cols-2">{rest.map(post => <Link href={`/blog/${post.slug}`} key={post.slug} className="group rounded-2xl border border-border bg-card p-7 transition-shadow hover:shadow-xl hover:shadow-primary/5"><div className="flex items-center justify-between text-xs text-muted-foreground"><span className="font-mono text-primary">{post.tags[0]}</span><span>{formatDate(post.date)}</span></div><h2 className="mt-16 text-2xl font-semibold tracking-tight group-hover:text-primary">{post.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{post.excerpt}</p><p className="mt-7 text-xs text-muted-foreground">{post.readTime}</p></Link>)}</div></div></main> }
